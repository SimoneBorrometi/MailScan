- pipeline.id: imap
  path.config: "/usr/share/logstash/pipeline/imap.conf"
  # pipeline.workers: 1
- pipeline.id: kafka_in
  path.config: "/usr/share/logstash/pipeline/kafka_input.conf"
  # queue.type: persisted